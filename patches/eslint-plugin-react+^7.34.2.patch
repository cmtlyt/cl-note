# generated by patch-package 6.4.14
#
# declared package:
#   eslint-plugin-react: ^7.34.2
#
diff --git a/node_modules/eslint-plugin-react/lib/rules/no-unknown-property.js b/node_modules/eslint-plugin-react/lib/rules/no-unknown-property.js
index 3e8c6de..f544177 100644
--- a/node_modules/eslint-plugin-react/lib/rules/no-unknown-property.js
+++ b/node_modules/eslint-plugin-react/lib/rules/no-unknown-property.js
@@ -533,6 +533,9 @@ module.exports = {
             type: 'string',
           },
         },
+        ignoreReg: {
+          type: 'RegExp'
+        },
         requireDataLowercase: {
           type: 'boolean',
           default: false,
@@ -553,11 +556,17 @@ module.exports = {
         : DEFAULTS.requireDataLowercase;
     }
 
+    function regCheck(actualName) {
+      const reg = context.options[0].ignoreReg
+      if(!reg) return false;
+      return reg.test(actualName)
+    }
+
     return {
       JSXAttribute(node) {
         const ignoreNames = getIgnoreConfig();
         const actualName = getText(context, node.name);
-        if (ignoreNames.indexOf(actualName) >= 0) {
+        if (ignoreNames.indexOf(actualName) >= 0 || regCheck(actualName)) {
           return;
         }
         const name = normalizeAttributeCase(actualName);
